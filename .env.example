# Nuclino API Key
# Can be obtained from your Nuclino profile page: https://app.nuclino.com/users/me
# This is used as a fallback if not provided via HTTP header for HTTP transport
# Required for stdio transport
NUCLINO_API_KEY=your-api-key-here

# Transport Type
# Options: 'http' or 'stdio'
TRANSPORT_TYPE=http

# HTTP Server Port (only used for HTTP transport)
PORT=3000